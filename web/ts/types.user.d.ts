export interface LeaderboardEntry {
    username: string,
    wpm: number,
    accurary: number,
    cpm: number,
    date: string
}

export interface HistoryEntry {
    wpm: number,
    accurary: number,
    cpm: number,
    date: string
}