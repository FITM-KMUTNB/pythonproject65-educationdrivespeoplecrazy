const isDark = window.matchMedia("(prefers-color-scheme: dark)").matches
if (isDark) {
    // document.head.insertAdjacentHTML('beforeend', '<link rel="stylesheet" href="https://bootswatch.com/5/darkly/bootstrap.min.css" id="theme-link">');
}

